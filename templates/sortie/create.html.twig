{% extends 'base.html.twig' %}

{% block title %}Nouvelle sortie{% endblock %}

{% block body %}
    <h1>Nouvelle sortie: </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Add!</button>
    {{ form_end(form) }}
{% endblock %}
